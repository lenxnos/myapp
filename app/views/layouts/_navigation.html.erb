<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to 'MyApp', root_path, class: 'navbar-brand', id: 'logo' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
       <li class="nav-item">
          <%= link_to 'Bloggers', users_path, class: "nav-link #{ 'active' if current_page?(users_path) }", "data-current" => "page" %>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Articles
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <% if logged_in? %>
              <li><%= link_to 'Create new article', new_article_path, class: "dropdown-item #{ 'active' if current_page?(new_article_path) }", "data-current" => "page" %></li>
            <% end %>
            <li><%= link_to 'View articles', articles_path, class: "dropdown-item #{ 'active' if current_page?(articles_path) }", "data-current" => "page" %></li>
          </ul>
        </li>
        <% if logged_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Profile [<%= current_user.username %>]
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <li><%= link_to 'View your profile', user_path(current_user), class: "dropdown-item #{ 'active' if current_page?(user_path(current_user)) }", "data-current" => "page" %></li>
              <li><%= link_to 'Edit your profile', edit_user_path(current_user), class: "dropdown-item #{ 'active' if current_page?(edit_user_path(current_user)) }", "data-current" => "page" %></li>
            </ul>
          </li>
          <li class="nav-item">
            <%= link_to 'Logout', logout_path, class: 'nav-link', data: { "turbo_method": :delete } %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Log in', login_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign up', signup_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>